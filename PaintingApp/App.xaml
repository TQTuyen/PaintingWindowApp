<?xml version="1.0" encoding="utf-8"?>
<Application
    x:Class="PaintingApp.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PaintingApp"
    xmlns:viewmodels="using:PaintingApp.ViewModels">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <XamlControlsResources xmlns="using:Microsoft.UI.Xaml.Controls" />
                <!-- Other merged dictionaries here -->
            </ResourceDictionary.MergedDictionaries>
            
            <!-- 
            ViewModelLocator provides a centralized location for resolving ViewModels from XAML.
            Usage: DataContext="{Binding MainViewModel, Source={StaticResource Locator}}"
            Note: The Locator is resolved from the DI container via App.GetService<ViewModelLocator>()
            -->
            
            <!-- Other app resources here -->
        </ResourceDictionary>
    </Application.Resources>
</Application>
