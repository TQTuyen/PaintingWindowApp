<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="PaintingApp.Views.MainScreenView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:entities="using:PaintingApp.Data.Entities"
    mc:Ignorable="d"
    Background="Transparent">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="Welcome to Painting App"
                       Style="{StaticResource TitleTextBlockStyle}"
                       Margin="0,0,0,8"/>
            <TextBlock Text="Select a profile to get started or create a new one"
                       Style="{StaticResource SubtitleTextBlockStyle}"
                       Opacity="0.7"/>
        </StackPanel>

        <!-- Action Bar -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="8" Margin="0,0,0,16">
            <Button Command="{x:Bind ViewModel.CreateProfileCommand}">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xE710;" FontSize="16"/>
                    <TextBlock Text="Create Profile"/>
                </StackPanel>
            </Button>
            <Button Command="{x:Bind ViewModel.LoadProfilesCommand}">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xE72C;" FontSize="16"/>
                    <TextBlock Text="Refresh"/>
                </StackPanel>
            </Button>
        </StackPanel>

        <!-- Loading Indicator -->
        <ProgressRing Grid.Row="2"
                      IsActive="{x:Bind ViewModel.IsBusy, Mode=OneWay}"
                      Width="48"
                      Height="48"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Center"/>

        <!-- Empty State -->
        <StackPanel Grid.Row="2"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center"
                    Visibility="{x:Bind ViewModel.Profiles.Count, Mode=OneWay, Converter={StaticResource CountToVisibilityConverter}, ConverterParameter=0}">
            <FontIcon Glyph="&#xE77B;" FontSize="64" Opacity="0.5"/>
            <TextBlock Text="No Profiles Found"
                       Style="{StaticResource SubtitleTextBlockStyle}"
                       HorizontalAlignment="Center"
                       Margin="0,16,0,8"/>
            <TextBlock Text="Create a new profile to get started"
                       Style="{StaticResource CaptionTextBlockStyle}"
                       Opacity="0.7"
                       HorizontalAlignment="Center"/>
        </StackPanel>

        <!-- Profile Cards Grid -->
        <GridView Grid.Row="2"
                  x:Name="ProfileGridView"
                  ItemsSource="{x:Bind ViewModel.Profiles, Mode=OneWay}"
                  SelectionMode="None"
                  IsItemClickEnabled="True"
                  ItemClick="ProfileGridView_ItemClick">
            <GridView.ItemsPanel>
                <ItemsPanelTemplate>
                    <ItemsWrapGrid Orientation="Horizontal"
                                   MaximumRowsOrColumns="4"
                                   ItemWidth="280"
                                   ItemHeight="180"/>
                </ItemsPanelTemplate>
            </GridView.ItemsPanel>
            <GridView.ItemTemplate>
                <DataTemplate x:DataType="entities:Profile">
                    <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1"
                            CornerRadius="8"
                            Padding="16"
                            Margin="4">
                        <Border.ContextFlyout>
                            <MenuFlyout>
                                <MenuFlyoutItem Text="Edit" Click="EditProfile_Click" Tag="{x:Bind}">
                                    <MenuFlyoutItem.Icon>
                                        <FontIcon Glyph="&#xE70F;"/>
                                    </MenuFlyoutItem.Icon>
                                </MenuFlyoutItem>
                                <MenuFlyoutItem Text="Delete" Click="DeleteProfile_Click" Tag="{x:Bind}">
                                    <MenuFlyoutItem.Icon>
                                        <FontIcon Glyph="&#xE74D;"/>
                                    </MenuFlyoutItem.Icon>
                                </MenuFlyoutItem>
                            </MenuFlyout>
                        </Border.ContextFlyout>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- Profile Icon and Name -->
                            <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="12">
                                <Border Background="{ThemeResource AccentFillColorDefaultBrush}"
                                        CornerRadius="20"
                                        Width="40"
                                        Height="40">
                                    <FontIcon Glyph="&#xE77B;"
                                              FontSize="20"
                                              Foreground="White"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                                </Border>
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock Text="{x:Bind Name}"
                                               Style="{StaticResource BodyStrongTextBlockStyle}"
                                               TextTrimming="CharacterEllipsis"
                                               MaxWidth="180"/>
                                    <TextBlock Text="{x:Bind Theme}"
                                               Style="{StaticResource CaptionTextBlockStyle}"
                                               Opacity="0.7"/>
                                </StackPanel>
                            </StackPanel>

                            <!-- Description -->
                            <TextBlock Grid.Row="1"
                                       Text="{x:Bind Description}"
                                       Style="{StaticResource CaptionTextBlockStyle}"
                                       TextWrapping="Wrap"
                                       TextTrimming="CharacterEllipsis"
                                       MaxLines="2"
                                       Opacity="0.8"
                                       Margin="0,12,0,0"/>

                            <!-- Canvas Info -->
                            <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="16" Margin="0,8,0,0">
                                <StackPanel Orientation="Horizontal" Spacing="4">
                                    <FontIcon Glyph="&#xE8B9;" FontSize="12" Opacity="0.6"/>
                                    <TextBlock Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.6">
                                        <Run Text="{x:Bind DefaultCanvasWidth}"/>
                                        <Run Text="x"/>
                                        <Run Text="{x:Bind DefaultCanvasHeight}"/>
                                    </TextBlock>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Spacing="4">
                                    <FontIcon Glyph="&#xEE56;" FontSize="12" Opacity="0.6"/>
                                    <TextBlock Text="{x:Bind DefaultStrokeThickness}"
                                               Style="{StaticResource CaptionTextBlockStyle}"
                                               Opacity="0.6"/>
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>
    </Grid>
</Page>
