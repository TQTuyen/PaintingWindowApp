<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="PaintingApp.Views.ManagementView"
    x:Name="PageRoot"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PaintingApp.Views"
    xmlns:viewmodels="using:PaintingApp.ViewModels"
    xmlns:models="using:PaintingApp.Models"
    xmlns:lvc="using:LiveChartsCore.SkiaSharpView.WinUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Transparent">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header with Breadcrumb -->
        <Border Grid.Row="0"
                Background="{ThemeResource AcrylicInAppFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="0,0,0,1"
                Padding="24,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <BreadcrumbBar Grid.Column="0"
                               ItemsSource="{x:Bind ViewModel.BreadcrumbItems, Mode=OneWay}"
                               VerticalAlignment="Center"/>

                <Button Grid.Column="1"
                        Command="{x:Bind ViewModel.RefreshAllCommand}"
                        ToolTipService.ToolTip="Refresh all data"
                        Padding="8,6">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE72C;" FontSize="14"/>
                        <TextBlock Text="Refresh"/>
                    </StackPanel>
                </Button>
            </Grid>
        </Border>

        <!-- Main Content with Tabs -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="280"/>
            </Grid.ColumnDefinitions>

            <!-- Tab Content Area with Acrylic Background -->
            <Border Grid.Column="0"
                    Background="{ThemeResource AcrylicInAppFillColorBaseBrush}">
                <TabView TabWidthMode="Equal"
                         IsAddTabButtonVisible="False"
                         CanDragTabs="False"
                         CanReorderTabs="False"
                         SelectedIndex="{x:Bind ViewModel.SelectedTabIndex, Mode=TwoWay}">

                    <!-- Boards Tab -->
                    <TabViewItem Header="Boards" IsClosable="False">
                        <TabViewItem.IconSource>
                            <FontIconSource Glyph="&#xE8D8;"/>
                        </TabViewItem.IconSource>
                        <TabViewItem.Content>
                            <Grid Padding="16">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <!-- Search and Actions Bar -->
                                <Grid Grid.Row="0" Margin="0,0,0,16">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="300"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <AutoSuggestBox Grid.Column="0"
                                                    PlaceholderText="Search boards..."
                                                    QueryIcon="Find"
                                                    Text="{x:Bind ViewModel.BoardSearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

                                    <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="8">
                                        <TextBlock Text="{x:Bind ViewModel.Boards.Count, Mode=OneWay}"
                                                   VerticalAlignment="Center"
                                                   Style="{StaticResource CaptionTextBlockStyle}"
                                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                        <TextBlock Text="boards"
                                                   VerticalAlignment="Center"
                                                   Style="{StaticResource CaptionTextBlockStyle}"
                                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                    </StackPanel>
                                </Grid>

                                <!-- Content Area -->
                                <Grid Grid.Row="1">
                                    <!-- Empty State -->
                                    <StackPanel Visibility="{x:Bind ViewModel.HasBoards, Mode=OneWay, Converter={StaticResource InverseBoolToVisibilityConverter}}"
                                                VerticalAlignment="Center"
                                                HorizontalAlignment="Center"
                                                Spacing="16">
                                        <FontIcon Glyph="&#xE8D8;" FontSize="64" Opacity="0.4"/>
                                        <TextBlock Text="No boards found"
                                                   Style="{StaticResource SubtitleTextBlockStyle}"
                                                   HorizontalAlignment="Center"
                                                   Opacity="0.7"/>
                                        <TextBlock Text="Create a new board from the Drawing screen to get started"
                                                   Style="{StaticResource CaptionTextBlockStyle}"
                                                   HorizontalAlignment="Center"
                                                   Opacity="0.5"/>
                                    </StackPanel>

                                    <!-- Boards Grid -->
                                    <GridView ItemsSource="{x:Bind ViewModel.Boards, Mode=OneWay}"
                                              SelectionMode="None"
                                              IsItemClickEnabled="True"
                                              Visibility="{x:Bind ViewModel.HasBoards, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                                        <GridView.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <ItemsWrapGrid Orientation="Horizontal" ItemWidth="300"/>
                                            </ItemsPanelTemplate>
                                        </GridView.ItemsPanel>
                                        <GridView.ItemTemplate>
                                            <DataTemplate x:DataType="viewmodels:BoardViewModel">
                                                <Grid Width="280"
                                                      Height="220"
                                                      Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                                      BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                                      BorderThickness="1"
                                                      CornerRadius="8"
                                                      Padding="16"
                                                      Margin="4">
                                                    <Grid.ContextFlyout>
                                                        <MenuFlyout>
                                                            <MenuFlyoutItem Text="Open"
                                                                            Command="{Binding ElementName=PageRoot, Path=ViewModel.OpenBoardCommand}"
                                                                            CommandParameter="{x:Bind}">
                                                                <MenuFlyoutItem.Icon>
                                                                    <FontIcon Glyph="&#xE8A7;"/>
                                                                </MenuFlyoutItem.Icon>
                                                            </MenuFlyoutItem>
                                                            <MenuFlyoutSeparator/>
                                                            <MenuFlyoutItem Text="Delete"
                                                                            Command="{Binding ElementName=PageRoot, Path=ViewModel.DeleteBoardCommand}"
                                                                            CommandParameter="{x:Bind}">
                                                                <MenuFlyoutItem.Icon>
                                                                    <FontIcon Glyph="&#xE74D;" Foreground="{ThemeResource SystemFillColorCriticalBrush}"/>
                                                                </MenuFlyoutItem.Icon>
                                                            </MenuFlyoutItem>
                                                        </MenuFlyout>
                                                    </Grid.ContextFlyout>
                                                    <Grid.RowDefinitions>
                                                        <RowDefinition Height="Auto"/>
                                                        <RowDefinition Height="*"/>
                                                        <RowDefinition Height="Auto"/>
                                                    </Grid.RowDefinitions>

                                                    <!-- Header -->
                                                    <Grid Grid.Row="0">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                        </Grid.ColumnDefinitions>

                                                        <TextBlock Grid.Column="0"
                                                                   Text="{x:Bind Name}"
                                                                   Style="{StaticResource SubtitleTextBlockStyle}"
                                                                   TextTrimming="CharacterEllipsis"
                                                                   MaxLines="1"/>

                                                        <Button Grid.Column="1"
                                                                Command="{Binding ElementName=PageRoot, Path=ViewModel.DeleteBoardCommand}"
                                                                CommandParameter="{x:Bind}"
                                                                Background="Transparent"
                                                                BorderThickness="0"
                                                                Padding="6"
                                                                ToolTipService.ToolTip="Delete Board">
                                                            <Button.Resources>
                                                                <SolidColorBrush x:Key="ButtonForegroundPointerOver" Color="{ThemeResource SystemFillColorCritical}"/>
                                                            </Button.Resources>
                                                            <FontIcon Glyph="&#xE74D;" FontSize="14"/>
                                                        </Button>
                                                    </Grid>

                                                    <!-- Preview Area -->
                                                    <Button Grid.Row="1"
                                                            Command="{Binding ElementName=PageRoot, Path=ViewModel.OpenBoardCommand}"
                                                            CommandParameter="{x:Bind}"
                                                            Padding="0"
                                                            Background="Transparent"
                                                            BorderThickness="0"
                                                            HorizontalAlignment="Stretch"
                                                            VerticalAlignment="Stretch"
                                                            Margin="0,12">
                                                        <Border Background="White"
                                                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                                                BorderThickness="1"
                                                                CornerRadius="4"
                                                                HorizontalAlignment="Stretch"
                                                                VerticalAlignment="Stretch">
                                                            <FontIcon Glyph="&#xE8D8;"
                                                                      FontSize="24"
                                                                      Opacity="0.3"
                                                                      HorizontalAlignment="Center"
                                                                      VerticalAlignment="Center"/>
                                                        </Border>
                                                    </Button>

                                                    <!-- Metadata -->
                                                    <StackPanel Grid.Row="2" Spacing="4">
                                                        <Grid>
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="*"/>
                                                                <ColumnDefinition Width="Auto"/>
                                                            </Grid.ColumnDefinitions>

                                                            <TextBlock Grid.Column="0"
                                                                       Style="{StaticResource CaptionTextBlockStyle}"
                                                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}">
                                                                <Run Text="{x:Bind SizeDisplay}"/>
                                                                <Run Text="•"/>
                                                                <Run Text="{x:Bind ShapeCount}"/>
                                                                <Run Text="shapes"/>
                                                            </TextBlock>
                                                        </Grid>

                                                        <TextBlock Style="{StaticResource CaptionTextBlockStyle}"
                                                                   Foreground="{ThemeResource TextFillColorTertiaryBrush}">
                                                            <Run Text="Modified:"/>
                                                            <Run Text="{x:Bind LastModified, Converter={StaticResource DateTimeToLocalStringConverter}}"/>
                                                        </TextBlock>
                                                    </StackPanel>
                                                </Grid>
                                            </DataTemplate>
                                        </GridView.ItemTemplate>
                                    </GridView>
                                </Grid>
                            </Grid>
                        </TabViewItem.Content>
                    </TabViewItem>

                    <!-- Templates Tab -->
                    <TabViewItem Header="Templates" IsClosable="False">
                        <TabViewItem.IconSource>
                            <FontIconSource Glyph="&#xE7C3;"/>
                        </TabViewItem.IconSource>
                        <TabViewItem.Content>
                            <Grid Padding="16">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <!-- Search and Actions Bar -->
                                <Grid Grid.Row="0" Margin="0,0,0,16">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="300"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <AutoSuggestBox Grid.Column="0"
                                                    PlaceholderText="Search templates..."
                                                    QueryIcon="Find"
                                                    Text="{x:Bind ViewModel.TemplateSearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

                                    <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="8">
                                        <TextBlock Text="{x:Bind ViewModel.Templates.Count, Mode=OneWay}"
                                                   VerticalAlignment="Center"
                                                   Style="{StaticResource CaptionTextBlockStyle}"
                                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                        <TextBlock Text="templates"
                                                   VerticalAlignment="Center"
                                                   Style="{StaticResource CaptionTextBlockStyle}"
                                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                    </StackPanel>
                                </Grid>

                                <!-- Content Area -->
                                <Grid Grid.Row="1">
                                    <!-- Empty State -->
                                    <StackPanel Visibility="{x:Bind ViewModel.HasTemplates, Mode=OneWay, Converter={StaticResource InverseBoolToVisibilityConverter}}"
                                                VerticalAlignment="Center"
                                                HorizontalAlignment="Center"
                                                Spacing="16">
                                        <FontIcon Glyph="&#xE7C3;" FontSize="64" Opacity="0.4"/>
                                        <TextBlock Text="No templates found"
                                                   Style="{StaticResource SubtitleTextBlockStyle}"
                                                   HorizontalAlignment="Center"
                                                   Opacity="0.7"/>
                                        <TextBlock Text="Select shapes on the Drawing screen and save them as a template"
                                                   Style="{StaticResource CaptionTextBlockStyle}"
                                                   HorizontalAlignment="Center"
                                                   Opacity="0.5"
                                                   TextWrapping="Wrap"
                                                   MaxWidth="300"/>
                                    </StackPanel>

                                    <!-- Templates Grid -->
                                    <GridView ItemsSource="{x:Bind ViewModel.Templates, Mode=OneWay}"
                                              SelectionMode="None"
                                              IsItemClickEnabled="True"
                                              Visibility="{x:Bind ViewModel.HasTemplates, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                                        <GridView.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <ItemsWrapGrid Orientation="Horizontal" ItemWidth="220"/>
                                            </ItemsPanelTemplate>
                                        </GridView.ItemsPanel>
                                        <GridView.ItemTemplate>
                                            <DataTemplate x:DataType="viewmodels:TemplateGroupViewModel">
                                                <Grid Width="200"
                                                      Height="200"
                                                      Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                                      BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                                      BorderThickness="1"
                                                      CornerRadius="8"
                                                      Padding="12"
                                                      Margin="4">
                                                    <Grid.ContextFlyout>
                                                        <MenuFlyout>
                                                            <MenuFlyoutItem Text="Delete"
                                                                            Command="{Binding ElementName=PageRoot, Path=ViewModel.DeleteTemplateCommand}"
                                                                            CommandParameter="{x:Bind}">
                                                                <MenuFlyoutItem.Icon>
                                                                    <FontIcon Glyph="&#xE74D;" Foreground="{ThemeResource SystemFillColorCriticalBrush}"/>
                                                                </MenuFlyoutItem.Icon>
                                                            </MenuFlyoutItem>
                                                        </MenuFlyout>
                                                    </Grid.ContextFlyout>
                                                    <Grid.RowDefinitions>
                                                        <RowDefinition Height="Auto"/>
                                                        <RowDefinition Height="*"/>
                                                        <RowDefinition Height="Auto"/>
                                                    </Grid.RowDefinitions>

                                                    <!-- Header -->
                                                    <Grid Grid.Row="0">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                        </Grid.ColumnDefinitions>

                                                        <TextBlock Grid.Column="0"
                                                                   Text="{x:Bind Name}"
                                                                   Style="{StaticResource BodyStrongTextBlockStyle}"
                                                                   TextTrimming="CharacterEllipsis"
                                                                   MaxLines="1"/>

                                                        <Button Grid.Column="1"
                                                                Command="{Binding ElementName=PageRoot, Path=ViewModel.DeleteTemplateCommand}"
                                                                CommandParameter="{x:Bind}"
                                                                Background="Transparent"
                                                                BorderThickness="0"
                                                                Padding="4"
                                                                ToolTipService.ToolTip="Delete Template">
                                                            <Button.Resources>
                                                                <SolidColorBrush x:Key="ButtonForegroundPointerOver" Color="{ThemeResource SystemFillColorCritical}"/>
                                                            </Button.Resources>
                                                            <FontIcon Glyph="&#xE74D;" FontSize="12"/>
                                                        </Button>
                                                    </Grid>

                                                    <!-- Thumbnail -->
                                                    <Border Grid.Row="1"
                                                            Background="{ThemeResource LayerFillColorDefaultBrush}"
                                                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                                            BorderThickness="1"
                                                            CornerRadius="4"
                                                            Margin="0,8">
                                                        <FontIcon Glyph="&#xE7C3;"
                                                                  FontSize="32"
                                                                  Opacity="0.3"
                                                                  HorizontalAlignment="Center"
                                                                  VerticalAlignment="Center"/>
                                                    </Border>

                                                    <!-- Info -->
                                                    <StackPanel Grid.Row="2" Spacing="2">
                                                        <TextBlock Style="{StaticResource CaptionTextBlockStyle}"
                                                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}">
                                                            <Run Text="{x:Bind ShapeCount}"/>
                                                            <Run Text="shapes"/>
                                                        </TextBlock>
                                                        <TextBlock Style="{StaticResource CaptionTextBlockStyle}"
                                                                   Foreground="{ThemeResource TextFillColorTertiaryBrush}">
                                                            <Run Text="Used"/>
                                                            <Run Text="{x:Bind UsageCount, Mode=OneWay}"/>
                                                            <Run Text="times"/>
                                                        </TextBlock>
                                                    </StackPanel>
                                                </Grid>
                                            </DataTemplate>
                                        </GridView.ItemTemplate>
                                    </GridView>
                                </Grid>
                            </Grid>
                        </TabViewItem.Content>
                    </TabViewItem>

                    <!-- Dashboard Tab -->
                    <TabViewItem Header="Dashboard" IsClosable="False">
                        <TabViewItem.IconSource>
                            <FontIconSource Glyph="&#xE9D2;"/>
                        </TabViewItem.IconSource>
                        <TabViewItem.Content>
                            <ScrollViewer VerticalScrollBarVisibility="Auto">
                                <Grid Padding="24">
                                    <!-- Loading Indicator -->
                                    <ProgressRing IsActive="{x:Bind ViewModel.IsDashboardLoading, Mode=OneWay}"
                                                  Width="48" Height="48"
                                                  HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"
                                                  Visibility="{x:Bind ViewModel.IsDashboardLoading, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"/>

                                    <!-- Dashboard Content -->
                                    <StackPanel Spacing="24"
                                                Visibility="{x:Bind ViewModel.IsDashboardLoading, Mode=OneWay, Converter={StaticResource InverseBoolToVisibilityConverter}}">
                                        
                                        <!-- Header with Refresh Button -->
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            
                                            <StackPanel Grid.Column="0">
                                                <TextBlock Text="Analytics Dashboard"
                                                           Style="{StaticResource TitleTextBlockStyle}"/>
                                                <TextBlock Text="Shape usage and template statistics for the current profile"
                                                           Style="{StaticResource CaptionTextBlockStyle}"
                                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                           Margin="0,4,0,0"/>
                                            </StackPanel>
                                            
                                            <Button Grid.Column="1"
                                                    Command="{x:Bind ViewModel.RefreshDashboardCommand}"
                                                    ToolTipService.ToolTip="Refresh dashboard data"
                                                    VerticalAlignment="Top">
                                                <StackPanel Orientation="Horizontal" Spacing="8">
                                                    <FontIcon Glyph="&#xE72C;" FontSize="14"/>
                                                    <TextBlock Text="Refresh"/>
                                                </StackPanel>
                                            </Button>
                                        </Grid>

                                        <!-- Summary Cards -->
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="16"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>

                                            <!-- Total Shapes Card -->
                                            <Border Grid.Column="0"
                                                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                                    BorderThickness="1"
                                                    CornerRadius="8"
                                                    Padding="20">
                                                <StackPanel>
                                                    <TextBlock Text="Total Shapes"
                                                               Style="{StaticResource CaptionTextBlockStyle}"
                                                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                                    <TextBlock Text="{x:Bind ViewModel.TotalShapeCount, Mode=OneWay}"
                                                               Style="{StaticResource TitleLargeTextBlockStyle}"
                                                               Margin="0,8,0,0"/>
                                                    <TextBlock Text="across all boards"
                                                               Style="{StaticResource CaptionTextBlockStyle}"
                                                               Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                                               Margin="0,4,0,0"/>
                                                </StackPanel>
                                            </Border>

                                            <!-- Total Template Usage Card -->
                                            <Border Grid.Column="2"
                                                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                                    BorderThickness="1"
                                                    CornerRadius="8"
                                                    Padding="20">
                                                <StackPanel>
                                                    <TextBlock Text="Template Insertions"
                                                               Style="{StaticResource CaptionTextBlockStyle}"
                                                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                                    <TextBlock Text="{x:Bind ViewModel.TotalTemplateUsageCount, Mode=OneWay}"
                                                               Style="{StaticResource TitleLargeTextBlockStyle}"
                                                               Margin="0,8,0,0"/>
                                                    <TextBlock Text="total uses"
                                                               Style="{StaticResource CaptionTextBlockStyle}"
                                                               Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                                               Margin="0,4,0,0"/>
                                                </StackPanel>
                                            </Border>
                                        </Grid>

                                        <!-- Charts Row -->
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="16"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>

                                            <!-- Shape Type Usage Pie Chart Card -->
                                            <Border Grid.Column="0"
                                                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                                    BorderThickness="1"
                                                    CornerRadius="8"
                                                    Padding="20"
                                                    MinHeight="400">
                                                <Grid>
                                                    <Grid.RowDefinitions>
                                                        <RowDefinition Height="Auto"/>
                                                        <RowDefinition Height="*"/>
                                                    </Grid.RowDefinitions>

                                                    <!-- Card Header -->
                                                    <StackPanel Grid.Row="0" Margin="0,0,0,16">
                                                        <TextBlock Text="Shape Type Distribution"
                                                                   Style="{StaticResource SubtitleTextBlockStyle}"/>
                                                        <TextBlock Text="Breakdown of shapes by type"
                                                                   Style="{StaticResource CaptionTextBlockStyle}"
                                                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                                   Margin="0,4,0,0"/>
                                                    </StackPanel>

                                                    <!-- Empty State -->
                                                    <StackPanel Grid.Row="1"
                                                                Visibility="{x:Bind ViewModel.HasShapeTypeData, Mode=OneWay, Converter={StaticResource InverseBoolToVisibilityConverter}}"
                                                                VerticalAlignment="Center"
                                                                HorizontalAlignment="Center"
                                                                Spacing="12">
                                                        <FontIcon Glyph="&#xE9D2;" FontSize="48" Opacity="0.3"/>
                                                        <TextBlock Text="No shape data available"
                                                                   Style="{StaticResource BodyTextBlockStyle}"
                                                                   Opacity="0.6"
                                                                   HorizontalAlignment="Center"/>
                                                        <TextBlock Text="Draw shapes on a board to see statistics"
                                                                   Style="{StaticResource CaptionTextBlockStyle}"
                                                                   Opacity="0.4"
                                                                   HorizontalAlignment="Center"/>
                                                    </StackPanel>

                                                    <!-- Pie Chart with LiveCharts2 -->
                                                    <lvc:PieChart Grid.Row="1"
                                                                  Series="{x:Bind ViewModel.ShapeTypeSeries, Mode=OneWay}"
                                                                  LegendPosition="Right"
                                                                  InitialRotation="-90"
                                                                  Visibility="{x:Bind ViewModel.HasShapeTypeData, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                                </Grid>
                                            </Border>

                                            <!-- Top Templates Bar Chart Card -->
                                            <Border Grid.Column="2"
                                                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                                    BorderThickness="1"
                                                    CornerRadius="8"
                                                    Padding="20"
                                                    MinHeight="400">
                                                <Grid>
                                                    <Grid.RowDefinitions>
                                                        <RowDefinition Height="Auto"/>
                                                        <RowDefinition Height="*"/>
                                                    </Grid.RowDefinitions>

                                                    <!-- Card Header -->
                                                    <StackPanel Grid.Row="0" Margin="0,0,0,16">
                                                        <TextBlock Text="Top 5 Most Used Templates"
                                                                   Style="{StaticResource SubtitleTextBlockStyle}"/>
                                                        <TextBlock Text="Templates ranked by usage count"
                                                                   Style="{StaticResource CaptionTextBlockStyle}"
                                                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                                   Margin="0,4,0,0"/>
                                                    </StackPanel>

                                                    <!-- Empty State -->
                                                    <StackPanel Grid.Row="1"
                                                                Visibility="{x:Bind ViewModel.HasTopTemplatesData, Mode=OneWay, Converter={StaticResource InverseBoolToVisibilityConverter}}"
                                                                VerticalAlignment="Center"
                                                                HorizontalAlignment="Center"
                                                                Spacing="12">
                                                        <FontIcon Glyph="&#xE7C3;" FontSize="48" Opacity="0.3"/>
                                                        <TextBlock Text="No template usage data"
                                                                   Style="{StaticResource BodyTextBlockStyle}"
                                                                   Opacity="0.6"
                                                                   HorizontalAlignment="Center"/>
                                                        <TextBlock Text="Use templates in your drawings to see statistics"
                                                                   Style="{StaticResource CaptionTextBlockStyle}"
                                                                   Opacity="0.4"
                                                                   HorizontalAlignment="Center"
                                                                   TextWrapping="Wrap"
                                                                   TextAlignment="Center"
                                                                   MaxWidth="200"/>
                                                    </StackPanel>

                                                    <!-- Bar Chart with LiveCharts2 -->
                                                    <lvc:CartesianChart Grid.Row="1"
                                                                        Series="{x:Bind ViewModel.TopTemplatesSeries, Mode=OneWay}"
                                                                        XAxes="{x:Bind ViewModel.TopTemplatesXAxes, Mode=OneWay}"
                                                                        YAxes="{x:Bind ViewModel.TopTemplatesYAxes, Mode=OneWay}"
                                                                        Visibility="{x:Bind ViewModel.HasTopTemplatesData, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                                </Grid>
                                            </Border>
                                        </Grid>

                                        <!-- Detailed Data Grid -->
                                        <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                                BorderThickness="1"
                                                CornerRadius="8"
                                                Padding="20"
                                                Visibility="{x:Bind ViewModel.HasTopTemplatesData, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                                            <Grid>
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="*"/>
                                                </Grid.RowDefinitions>

                                                <!-- Header -->
                                                <StackPanel Grid.Row="0" Margin="0,0,0,16">
                                                    <TextBlock Text="Template Usage Details"
                                                               Style="{StaticResource SubtitleTextBlockStyle}"/>
                                                    <TextBlock Text="Detailed breakdown of template insertions"
                                                               Style="{StaticResource CaptionTextBlockStyle}"
                                                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                               Margin="0,4,0,0"/>
                                                </StackPanel>

                                                <!-- Data Table -->
                                                <Grid Grid.Row="1">
                                                    <Grid.RowDefinitions>
                                                        <RowDefinition Height="Auto"/>
                                                        <RowDefinition Height="*"/>
                                                    </Grid.RowDefinitions>

                                                    <!-- Table Header -->
                                                    <Border Grid.Row="0"
                                                            Background="{ThemeResource LayerFillColorDefaultBrush}"
                                                            CornerRadius="4"
                                                            Padding="12,8"
                                                            Margin="0,0,0,4">
                                                        <Grid>
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="40"/>
                                                                <ColumnDefinition Width="*"/>
                                                                <ColumnDefinition Width="100"/>
                                                                <ColumnDefinition Width="100"/>
                                                            </Grid.ColumnDefinitions>

                                                            <TextBlock Grid.Column="0" Text="#"
                                                                       Style="{StaticResource CaptionTextBlockStyle}"
                                                                       FontWeight="SemiBold"
                                                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                                            <TextBlock Grid.Column="1" Text="Template Name"
                                                                       Style="{StaticResource CaptionTextBlockStyle}"
                                                                       FontWeight="SemiBold"
                                                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                                            <TextBlock Grid.Column="2" Text="Usage Count"
                                                                       Style="{StaticResource CaptionTextBlockStyle}"
                                                                       FontWeight="SemiBold"
                                                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                                       HorizontalAlignment="Right"/>
                                                            <TextBlock Grid.Column="3" Text="Percentage"
                                                                       Style="{StaticResource CaptionTextBlockStyle}"
                                                                       FontWeight="SemiBold"
                                                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                                       HorizontalAlignment="Right"/>
                                                        </Grid>
                                                    </Border>

                                                    <!-- Table Rows -->
                                                    <ItemsControl Grid.Row="1"
                                                                  ItemsSource="{x:Bind ViewModel.TopTemplates, Mode=OneWay}">
                                                        <ItemsControl.ItemTemplate>
                                                            <DataTemplate x:DataType="models:TemplateUsageItem">
                                                                <Border Padding="12,10"
                                                                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                                                        BorderThickness="0,0,0,1">
                                                                    <Grid>
                                                                        <Grid.ColumnDefinitions>
                                                                            <ColumnDefinition Width="40"/>
                                                                            <ColumnDefinition Width="*"/>
                                                                            <ColumnDefinition Width="100"/>
                                                                            <ColumnDefinition Width="100"/>
                                                                        </Grid.ColumnDefinitions>

                                                                        <Ellipse Grid.Column="0" 
                                                                                 Width="24" Height="24"
                                                                                 VerticalAlignment="Center">
                                                                            <Ellipse.Fill>
                                                                                <SolidColorBrush Color="{x:Bind Color}"/>
                                                                            </Ellipse.Fill>
                                                                        </Ellipse>
                                                                        
                                                                        <TextBlock Grid.Column="1"
                                                                                   Text="{x:Bind TemplateName}"
                                                                                   Style="{StaticResource BodyTextBlockStyle}"
                                                                                   VerticalAlignment="Center"
                                                                                   TextTrimming="CharacterEllipsis"/>
                                                                        
                                                                        <TextBlock Grid.Column="2"
                                                                                   Text="{x:Bind UsageCount}"
                                                                                   Style="{StaticResource BodyStrongTextBlockStyle}"
                                                                                   VerticalAlignment="Center"
                                                                                   HorizontalAlignment="Right"/>
                                                                        
                                                                        <TextBlock Grid.Column="3"
                                                                                   Text="{x:Bind Percentage, Converter={StaticResource DoubleToPercentageConverter}}"
                                                                                   Style="{StaticResource BodyTextBlockStyle}"
                                                                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                                                   VerticalAlignment="Center"
                                                                                   HorizontalAlignment="Right"/>
                                                                    </Grid>
                                                                </Border>
                                                            </DataTemplate>
                                                        </ItemsControl.ItemTemplate>
                                                    </ItemsControl>
                                                </Grid>
                                            </Grid>
                                        </Border>
                                    </StackPanel>
                                </Grid>
                            </ScrollViewer>
                        </TabViewItem.Content>
                    </TabViewItem>
                </TabView>
            </Border>

            <!-- Quick Templates Sidebar -->
            <Border Grid.Column="1"
                    Background="{ThemeResource LayerFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1,0,0,0">
                <Grid Padding="16">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Header -->
                    <StackPanel Grid.Row="0" Spacing="4" Margin="0,0,0,16">
                        <TextBlock Text="Quick Templates"
                                   Style="{StaticResource SubtitleTextBlockStyle}"/>
                        <TextBlock Text="Most used templates"
                                   Style="{StaticResource CaptionTextBlockStyle}"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    </StackPanel>

                    <!-- Templates List -->
                    <Grid Grid.Row="1">
                        <!-- Empty State -->
                        <StackPanel Visibility="{x:Bind ViewModel.HasTemplatesLoaded, Mode=OneWay, Converter={StaticResource InverseBoolToVisibilityConverter}}"
                                    VerticalAlignment="Center"
                                    HorizontalAlignment="Center"
                                    Spacing="12">
                            <FontIcon Glyph="&#xE7C3;" FontSize="32" Opacity="0.3"/>
                            <TextBlock Text="No templates yet"
                                       Style="{StaticResource CaptionTextBlockStyle}"
                                       HorizontalAlignment="Center"
                                       Opacity="0.5"/>
                        </StackPanel>

                        <!-- Templates ListView -->
                        <ListView ItemsSource="{x:Bind ViewModel.Templates, Mode=OneWay}"
                                  SelectionMode="None"
                                  Visibility="{x:Bind ViewModel.HasTemplatesLoaded, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                            <ListView.ItemTemplate>
                                <DataTemplate x:DataType="viewmodels:TemplateGroupViewModel">
                                    <Grid Padding="8,12" ColumnSpacing="12">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="40"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <!-- Thumbnail -->
                                        <Border Grid.Column="0"
                                                Width="40"
                                                Height="40"
                                                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                                BorderThickness="1"
                                                CornerRadius="4">
                                            <FontIcon Glyph="&#xE7C3;" FontSize="16" Opacity="0.5"/>
                                        </Border>

                                        <!-- Info -->
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="2">
                                            <TextBlock Text="{x:Bind Name}"
                                                       Style="{StaticResource BodyTextBlockStyle}"
                                                       TextTrimming="CharacterEllipsis"
                                                       MaxLines="1"/>
                                            <TextBlock Style="{StaticResource CaptionTextBlockStyle}"
                                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}">
                                                <Run Text="{x:Bind ShapeCount}"/>
                                                <Run Text="shapes"/>
                                            </TextBlock>
                                        </StackPanel>

                                        <!-- Usage Count Badge -->
                                        <Border Grid.Column="2"
                                                Background="{ThemeResource AccentFillColorDefaultBrush}"
                                                CornerRadius="10"
                                                Padding="8,2"
                                                VerticalAlignment="Center">
                                            <TextBlock Text="{x:Bind UsageCount, Mode=OneWay}"
                                                       Style="{StaticResource CaptionTextBlockStyle}"
                                                       Foreground="White"/>
                                        </Border>
                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </Grid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Page>
